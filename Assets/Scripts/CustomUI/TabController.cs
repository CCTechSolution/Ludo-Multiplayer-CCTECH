using System.Collections;
using System.Collections.Generic;
using HK;
using UnityEngine;

public class TabController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
//        OnHome(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*

    public void OnHome(bool val)
    {
        if(val)
            Events.RequestMainMenu.Call();

    }

    public void OnFriends(bool val)
    {
        if (val)
            Events.RequestShop.Call();

    }

    public void OnEquipments(bool val)
    {
        if (val)
            Events.RequestEquipments.Call(0);

    }

    public void OnShop(bool val)
    {
        if (val)
            Events.RequestShop.Call();

    }
    */
}
